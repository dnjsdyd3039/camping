<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.camping.dao.MemberDao" >

<select id="memberIdCheck" resultType="String">
 SELECT MID
 FROM MEMBERS
 WHERE MID = #{inputId}
</select>

<insert id="memberJoin" >
 INSERT INTO MEMBERS(MID, MPW, MNAME, MTEL, MEMAIL, MBIRTH, MADDR, MSTATE, MRANK) 
 VALUES(#{mid}, #{mpw}, #{mname}, #{mtel}, #{memail}, #{mbirth}, #{maddr}, 'GN01', 'NEW')
</insert>

<select id="MaxGdcode" resultType="String">
SELECT MAX(GCODE)
FROM GOODS
</select>

</mapper>